specVersion: "v1"
serviceId: "secure-doc-share"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "secure-doc-share-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
